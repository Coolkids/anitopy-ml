# 第三方组件与许可证

## 旧版 Anitopy 基线

兼容模式和基线评测可加载旧项目 `Anitopy` 的冻结提交：

- 来源：`E:\Code\anitopy` 的提交 `48bd0ad6f0d074c54e1694ee85d96828fd043209`
- 许可证：Mozilla Public License Version 2.0（MPL-2.0）
- 原作者：Igor Cescon de Moura

当前项目不会将旧实现代码复制进发布包，也不会默认依赖开发机器上的绝对路径。使用旧解析器时，调用方通过 `source` 参数显式提供包含 `anitopy/` 和 `LICENSE` 的源目录；使用、复制或分发旧代码时应保留其许可证与版权声明。

后续加入的模型、TMDB客户端及其他依赖会在本文件中补充版本、许可证和用途信息。
